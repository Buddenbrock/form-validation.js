class FormValidation {
    constructor(options = {}) {
      this.initValidation();
    }

    /**
     * @desc init validation
     */
    initValidation = () => {
      console.log('Hello world');
    }
}
